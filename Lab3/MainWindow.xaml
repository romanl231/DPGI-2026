<Window x:Class="Lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ворожка" Height="350" Width="400"
        MinHeight="300" MinWidth="350">

    <Window.Resources>
        <LinearGradientBrush x:Key="BgBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1a1a2e" Offset="0"/>
            <GradientStop Color="#16213e" Offset="0.5"/>
            <GradientStop Color="#0f3460" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20 8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c73652"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🔮 Ворожка" FontSize="28" FontWeight="Bold"
                   Foreground="White" HorizontalAlignment="Center" Margin="0 24 0 8"/>

        <Border Grid.Row="1" Margin="24 8" CornerRadius="8"
                BorderBrush="#e94560" BorderThickness="1">
            <TextBox Name="txtQuestion" Margin="0"
                     Background="Transparent" Foreground="White"
                     CaretBrush="White" FontSize="14" Padding="12"
                     BorderThickness="0"
                     Text="Введіть ваше запитання..."
                     GotFocus="TxtQuestion_GotFocus"
                     LostFocus="TxtQuestion_LostFocus"/>
        </Border>

        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Name="lblAnswer" Text="?" FontSize="48" FontWeight="Bold"
                       Foreground="#e94560" HorizontalAlignment="Center"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Запитати" Margin="24 8 24 24"
                Name="btnAsk" Click="BtnAsk_Click"/>
    </Grid>
</Window>
